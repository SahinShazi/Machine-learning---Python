# Python While Loops

## While Loop কি জিনিস?

While loop মানে হলো - যতক্ষণ একটা শর্ত সত্যি ততক্ষণ একই কাজ বারবার করো। শর্ত মিথ্যা হলে থেমে যাও।

```python
while শর্ত:
    # কাজ করো
```

সহজ উদাহরণ - ধরো তুমি গুনতে চাও ১ থেকে ৫ পর্যন্ত:

```python
i = 1
while i <= 5:
    print(i)
    i = i + 1
```

এখানে যতক্ষণ i ছোট বা সমান ৫, ততক্ষণ প্রিন্ট করবে আর i বাড়াবে।

## একটা বড় সমস্যা - Infinite Loop

দেখো, while loop এ একটা ভয়ংকর জিনিস হতে পারে। যদি তুমি counter বাড়াতে ভুলে যাও:

```python
i = 1
while i <= 5:
    print(i)
    # এখানে i বাড়ানো হয়নি!
```

এটা চলতেই থাকবে। কখনো থামবে না। তাই সাবধান!

---

## Problem 01: ১ থেকে ১০ পর্যন্ত প্রিন্ট করা

এটা খুবই সহজ একটা সমস্যা। শুধু গুনতে হবে।

```python
num = 1

while num <= 10:
    print("The number is", num)
    num = num + 1
```

**কি হচ্ছে এখানে:**
- num শুরু করছি ১ দিয়ে
- যতক্ষণ num দশের সমান বা ছোট, ততক্ষণ চলবে
- প্রতিবার প্রিন্ট করে num একটা বাড়িয়ে দিচ্ছি

আউটপুট দেখবে ১ থেকে ১০ পর্যন্ত সব সংখ্যা।

---

## Problem 02: ১০ থেকে ০ পর্যন্ত উল্টো

এবার আমরা কমাবো, বাড়াবো না।

```python
num = 10

while num >= 0:
    print(num)
    num = num - 1
```

দেখো এখানে `num >= 0` দিয়েছি। মানে num যতক্ষণ ০ বা তার বেশি থাকবে ততক্ষণ চলবে। আর প্রতিবার ১ করে কমছে।

---

## Problem 03: জোড় সংখ্যা খুঁজে বের করা

১ থেকে ৫০ পর্যন্ত শুধু জোড় সংখ্যা দেখতে চাই।

```python
num = 1

while num <= 50:
    if num % 2 == 0:
        print(num)
    num = num + 1
```

মজার ব্যাপার হলো আমরা সব সংখ্যা চেক করছি, কিন্তু প্রিন্ট করছি শুধু জোড়গুলো। `num % 2 == 0` মানে হলো ২ দিয়ে ভাগ করলে ভাগশেষ ০।

আরেকটা উপায় আছে - সরাসরি ২ থেকে শুরু করে ২ করে বাড়াও:

```python
num = 2
while num <= 50:
    print(num)
    num = num + 2
```

এটা একটু ফাস্ট কারণ সব সংখ্যা চেক করতে হচ্ছে না।

---

## Problem 04: ১ থেকে ১০০ পর্যন্ত যোগফল

এখন আমাদের একটা ভ্যারিয়েবল লাগবে যেখানে আমরা যোগ করতে থাকবো।

```python
i = 1
sum = 0

while i <= 100:
    sum = sum + i
    i += 1

print("The sum is", sum)
```

**কিভাবে কাজ করছে:**
- sum শুরু করছি ০ দিয়ে
- প্রতিবার i এর মান sum এ যোগ করছি
- শেষে পুরো যোগফল প্রিন্ট করছি

উত্তর আসবে ৫০৫০। মজার না?

---

## Problem 05: গুণন টেবিল

ধরো ৭ এর টেবিল বানাতে হবে।

```python
n = 7
i = 1

while i <= 10:
    result = n * i
    print(f"{n} x {i} = {result}")
    i += 1
```

এটা দেখতে পাবে:
```
7 x 1 = 7
7 x 2 = 14
7 x 3 = 21
...
```

f-string ব্যবহার করেছি এখানে। এটা দিয়ে সুন্দর করে লেখা যায়।

---

## Problem 06: বর্গ বের করা

১ থেকে ১০ পর্যন্ত প্রতিটা সংখ্যার বর্গ (square) চাই।

```python
i = 1

while i <= 10:
    print(f"{i} এর বর্গ = {i * i}")
    i += 1
```

খুবই সহজ। শুধু `i * i` করলেই হলো। চাইলে `i ** 2` ও লিখতে পারো।

---

## Problem 07: সংখ্যা উল্টানো (Tricky!)

এটা একটু মাথা খাটাতে হবে। ১২৩৪ কে ৪৩২১ বানাতে হবে।

```python
num = 1234
reverse_num = 0

while num > 0:
    digit = num % 10
    reverse_num = reverse_num * 10 + digit
    num = num // 10

print(reverse_num)
```

**এটা কিভাবে কাজ করছে বুঝি:**

ধরো num = 1234

প্রথমবার:
- শেষ digit নিলাম: 1234 % 10 = 4
- reverse_num এ রাখলাম: 0 * 10 + 4 = 4
- শেষ digit সরালাম: 1234 // 10 = 123

দ্বিতীয়বার:
- শেষ digit: 123 % 10 = 3
- reverse_num: 4 * 10 + 3 = 43
- বাকি: 123 // 10 = 12

তৃতীয়বার:
- digit: 12 % 10 = 2
- reverse_num: 43 * 10 + 2 = 432
- বাকি: 12 // 10 = 1

চতুর্থবার:
- digit: 1 % 10 = 1
- reverse_num: 432 * 10 + 1 = 4321
- বাকি: 1 // 10 = 0

num = 0 হয়ে গেছে তাই লুপ শেষ!

---

## Problem 08: Factorial বের করা

৫! মানে হলো ৫ × ৪ × ৩ × ২ × ১ = ১২০

```python
n = 5
factorial = 1
i = n

while i > 0:
    factorial = factorial * i
    i -= 1

print(f"{n}! = {factorial}")
```

**একটু ব্যাখ্যা করি:**
- factorial শুরু করছি ১ দিয়ে (০ দিলে সব ০ হয়ে যাবে!)
- i শুরু করছি n দিয়ে
- প্রতিবার factorial এর সাথে i গুণ করছি
- i কমিয়ে দিচ্ছি

এভাবে ৫ থেকে শুরু করে ১ পর্যন্ত সব গুণ হয়ে যাচ্ছে।

---

## কিছু সাধারণ ভুল

### ভুল নাম্বার ১ - Counter বাড়াতে ভুলে যাওয়া

```python
i = 1
while i <= 5:
    print(i)
    # এখানে i += 1 নেই!
```

এটা infinite loop। থামবে না।

### ভুল নাম্বার ২ - শর্ত ভুল দেওয়া

```python
i = 10
while i < 5:  # এটা তো শুরুতেই মিথ্যা!
    print(i)
```

এটা একবারও চলবে না।

### ভুল নাম্বার ৩ - Off by one

```python
i = 1
while i < 10:  # এটা ৯ পর্যন্ত যাবে
    print(i)
    i += 1
```

যদি ১০ পর্যন্ত চাও তাহলে `i <= 10` দিতে হবে।

---

## While vs For - কোনটা কখন?

সত্যি কথা বলতে, for loop অনেক সময় সহজ:

```python
# While দিয়ে
i = 0
while i < 5:
    print(i)
    i += 1

# For দিয়ে (সহজ!)
for i in range(5):
    print(i)
```

তাহলে while কখন ব্যবহার করবে? যখন জানো না ঠিক কতবার চলবে। যেমন:

```python
password = ""
while password != "secret":
    password = input("Enter password: ")
```

এখানে তো জানো না ইউজার কতবারে সঠিক পাসওয়ার্ড দিবে!

---

## কিছু টিপস

১. সবসময় নিশ্চিত করো loop কোনো এক সময় শেষ হবে
২. Counter ঠিকমতো বাড়ছে কিনা চেক করো
৩. প্রথমে কাগজে লিখে বুঝে নাও কিভাবে কাজ করবে
৪. সন্দেহ হলে print করে দেখো কি হচ্ছে

```python
i = 1
while i <= 5:
    print(f"i এর মান: {i}")  # Debug করতে পারো এভাবে
    i += 1
```

---

## অনুশীলন করো

১. **প্রাইম নাম্বার**: একটা সংখ্যা প্রাইম কিনা বের করো
২. **Fibonacci**: ০, ১, ১, ২, ৩, ৫, ৮... এভাবে প্রথম ১০টা সংখ্যা বের করো
৩. **Digit যোগ**: ১২৩ এর সব digit যোগ করো (১+২+৩ = ৬)
৪. **পাসওয়ার্ড গেম**: সঠিক পাসওয়ার্ড না দিলে বারবার চাইতে থাকো

---

মনে রাখো, while loop খুব শক্তিশালী জিনিস কিন্তু একটু সাবধানে চালাতে হয়। প্র্যাকটিস করলে সব ক্লিয়ার হয়ে যাবে!

Happy Coding! 🚀